# SPDX-License-Identifier: MIT
# Development and CI tooling. Runtime dependencies live in python/requirements.txt
# and mcp_servers/*/requirements.txt.

# Lint / format / types
ruff~=0.6.9
mypy~=1.11.2
types-requests~=2.32.0
types-PyYAML~=6.0.12

# Tests
pytest~=9.1.1
pytest-cov~=5.0.0
responses~=0.25.3

# Security
bandit~=1.7.10
pip-audit~=2.10.1

# Ansible tooling. ansible-core 2.21 and ansible-lint 26 declare Requires-Python >=3.12,
# so they are skipped on 3.11, where the rest of the suite still runs.
ansible-core~=2.21.3 ; python_version >= "3.12"
ansible-lint~=26.8.0 ; python_version >= "3.12"

# Pre-commit
pre-commit~=3.8.0
